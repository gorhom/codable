import {
  stringType,
  numberType,
  booleanType,
  arrayType,
  optionalType,
} from '../internal';

export const types = {
  string: stringType,
  number: numberType,
  boolean: booleanType,
  array: arrayType,
  optional: optionalType,
};
